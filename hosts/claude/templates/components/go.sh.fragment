# SEED:start go
score_go() {
  if ! command -v go >/dev/null 2>&1; then
    echo "missing: go" >&2
    return 2
  fi
  if go test ./... >/dev/null 2>&1; then
    echo "1.0"
  else
    echo "0.0"
  fi
}
# SEED:end go
