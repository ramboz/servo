# Secrets / deploy-touching brief → needs_tightening (safety surface named)

Rotate the production database credentials, update the deploy pipeline's secret
store with the new values, and roll the change out to the production environment
automatically once the smoke tests pass.
